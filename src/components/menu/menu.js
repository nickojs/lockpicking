import React from 'react';
import * as S from './styles';

const menu = (props) => (
  <nav>
    <ul>
      <li>Play</li>
      <li>Stats</li>
      <li>Login</li>
      <li>About</li>
    </ul>
  </nav>
);

export default menu;
