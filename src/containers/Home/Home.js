import React from 'react';
import Menu from '../../components/menu/menu';

const Home = (props) => (
  <div>
    <h1>Skyrim</h1>
    <p>lockpick simulator</p>
    <p>Press any key to start</p>
    <Menu />
  </div>
);

export default Home;
